<div class="container mx-auto px-4 py-8">
  <h1 class="text-4xl font-bold text-center text-blue-600 mb-6">Articles</h1>

  <p class="text-center text-gray-700 text-lg mb-4">
    Our blog has <span class="font-semibold text-blue-500"><%= Article.public_count %></span> articles and counting!
  </p>

  <ul class="list-none space-y-2">
    <% @articles.each do |article| %>
      <% unless article.archived? %>
        <li class="bg-white shadow rounded-lg p-4 hover:bg-gray-50 transition">
          <%= link_to article.title, article, class: "text-blue-500 hover:text-blue-700 font-semibold" %>
        </li>
      <% end %>
    <% end %>
  </ul>

  <div class="text-center mt-6">
    <%= link_to "New Article", new_article_path, class: "bg-blue-500 text-white font-bold py-2 px-4 rounded hover:bg-blue-600 transition" %>
  </div>
</div>
